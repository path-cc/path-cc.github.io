{% assign news = site.news %}

{% assign spotlights = site.articles | where_exp: "article", "article.publish_on contains site.key" | where_exp: "article", "article.type == 'news'" %}

{% for story in spotlights %}
        {% assign news = news | push: story %}
{% endfor %}
{% assign news = news | sort: "date" | reverse %}
